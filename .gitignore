package-lock.json
*.js
node_modules
bin/

# swap-scene.mjs backup of scene.json before variant swap
scene.json.bak

# Local webhook fallback (Genesis deploy workaround — see webhookSecrets.example.ts)
src/server/webhookSecrets.ts
.DS_Store
**/.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# env
.env
.env.local

# runtime
.runtime-data/
node_modules/@dcl/sdk-commands/.runtime-data/

# canvas state backups (large, volatile, dated)
backups/
backups/raw-*.log

# Timelapse frames + rendered video (regenerated from Discord archive)
timelapse/

# 3D source files (Blender / SketchUp) — exports live in assets/models/*.glb
assets/blender/
assets/sketchup/
